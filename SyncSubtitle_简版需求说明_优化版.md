# 简版需求说明

## 一、产品背景

部分外语学习者希望通过影视作品、番剧等内容学习日语，但自动生成字幕往往存在同音词识别错误、专有名词识别不准确等问题。

同时，一些用户虽然已经获得了较准确的字幕文件，但仍希望直接在 B 站等平台观看视频，继续使用平台原有的弹幕、倍速、收藏等功能，而不想切换到本地播放器加载字幕。

## 二、目标用户

第一版主要面向正在学习日语、希望通过影视作品积累词汇和提升听力，但现阶段仍需要字幕辅助的学习者。

## 三、使用场景

用户准备在 B 站或其他在线视频平台观看日语影视作品或番剧，并提前获得了对应的日语字幕文件。

观看过程中，用户希望程序能够根据电脑当前播放的音频，自动定位字幕内容，并通过悬浮窗口实时显示对应字幕。

## 四、核心痛点

- 平台自动生成的字幕准确率不稳定。
- 同音词、人名、地名和作品专有名词容易识别错误。
- 已有字幕文件无法直接加载到部分在线视频平台。
- 使用本地播放器会失去弹幕、收藏、评论等平台功能。
- 用户听力不足时，容易跟不上对白和剧情节奏。

## 五、产品目标

让用户在不切换原有视频平台的情况下，使用提前准备好的字幕文件辅助观看日语影视内容。

通过系统音频识别和字幕文本匹配，自动定位当前对白，并在透明悬浮窗口中显示对应字幕，从而提高观看体验和外语学习效率。

## 六、核心方案

用户提前上传与当前视频对应的日语 SRT 字幕文件。

点击“开始监听”后，程序通过 WASAPI Loopback 捕获电脑正在播放的系统音频，并使用日语语音识别模型生成短文本。

程序将识别结果与字幕内容进行匹配，确定当前播放位置，并在透明、置顶的悬浮字幕窗口中显示对应字幕。

## 七、MVP 功能范围

第一版需要实现：

- 支持上传和解析日语 SRT 字幕文件。
- 支持捕获 Windows 系统播放音频。
- 使用本地 Whisper 模型识别日语语音。
- 将识别文本与字幕内容进行模糊匹配。
- 自动记录当前字幕位置，并优先向后搜索。
- 连续匹配失败时扩大搜索范围，重新定位。
- 通过透明悬浮窗口显示当前字幕。
- 支持开始、暂停和停止监听。
- 支持手动切换上一句、下一句字幕。
- 支持调整字幕字号、位置、透明度和置顶状态。
- 显示当前识别文本、匹配字幕和匹配相似度。

第一版暂不实现：

- 英语及其他语言识别。
- 自动翻译和双语字幕。
- ASS 字幕特效。
- 在线字幕搜索和下载。
- 视频播放器和视频下载功能。

## 八、用户主流程

```text
打开程序
  ↓
上传对应的日语 SRT 字幕
  ↓
程序解析字幕并显示文件信息
  ↓
选择系统音频设备和识别模型
  ↓
点击“开始监听”
  ↓
捕获电脑正在播放的音频
  ↓
识别日语语音并匹配字幕
  ↓
悬浮窗口显示当前字幕
  ↓
用户继续观看视频
  ↓
暂停、停止或更换字幕文件
```

异常流程：

- 匹配失败：继续显示上一句，并重新识别。
- 连续多次失败：扩大字幕搜索范围。
- 用户拖动视频进度：重新进行全文定位。
- 音频设备不可用：提示用户重新选择设备。

## 九、核心页面

### 1. 实时同步页面

作为程序主页面，包含：

- 字幕文件上传区域。
- 已上传字幕文件信息。
- 系统音频设备选择。
- 日语识别模型选择。
- 开始、暂停和停止按钮。
- 当前识别文本。
- 当前匹配字幕。
- 匹配相似度和字幕序号。
- 音频监听状态。

### 2. 悬浮字幕窗口

用于覆盖在视频播放器上方，包含：

- 当前字幕文本。
- 透明背景。
- 窗口置顶。
- 鼠标穿透。
- 拖动调整位置。
- 字号、描边和透明度调整。

### 3. 设置页面

包含：

- 默认音频设备。
- Whisper 模型和运行设备。
- 识别片段时长。
- 字幕匹配阈值。
- 局部搜索范围。
- 字体、字号和窗口透明度。
- 配置保存与恢复默认设置。

## 十、成功标准

MVP 可以满足以下标准时，视为基本可用：

- 用户能够成功上传并解析日语 SRT 字幕。
- 程序能够稳定捕获 Windows 系统音频。
- 正常连续播放时，大部分字幕能够自动跟随对白切换。
- 从识别音频到显示字幕的延迟控制在约 2～4 秒以内。
- 对白识别存在少量错误时，仍能定位到正确或相邻字幕。
- 用户拖动播放进度后，程序能够在合理时间内重新定位。
- 连续观看过程中，不频繁跳转到错误的字幕位置。
- 悬浮字幕窗口不会明显影响用户操作视频播放器。
